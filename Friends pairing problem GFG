class Solution {
  public:
    int countFriendsPairings(int n) {
        vector<int> dp(n);
        dp[0]=1;
        dp[1]=2;
        for(int i=0; i<n-2; i++){
            dp[i+2]=((dp[i]*(i+2))+dp[i+1]);
        }
        return dp[n-1];
    }
};
