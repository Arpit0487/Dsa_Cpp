class Solution {
public:
    int maxOperations(string s) {
        int ones = 0;
        int ans = 0;
        int n = s.length();
        for(int i=0; i<s.length(); i++){
            if(s[i]=='1') ones++;
            else if(i+1==n || s[i+1]=='1') ans+=ones;
        }
        return ans;
    }
};
