class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int ans = 0;
        int net = 0;
        for(auto x : gain){
            net += x;
            ans = max(ans,net);
        }
        return ans;
    }
};
