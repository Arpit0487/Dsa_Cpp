class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int operations = 0;
        for(auto x : nums){
            if(x%3 <= 1) operations += (x%3);
            else operations += 3-(x%3);
        }
        return operations;
    }
};
